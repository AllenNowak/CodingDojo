# primary controller .py
# by convention the filename is pluralized as 'thing's_controller.py
# this matches a singularly named 'thing'_model
from flask_app import app
from flask import render_template, redirect, request, response, session
# from someFile import SomeClass for this controller's model

# include all the @app routes

